[reticulum]
  enable_transport = No
  share_instance = No
  instance_name = rns-mqtt-b

[logging]
  loglevel = 4

[interfaces]
  [[MQTT virtual node B]]
    type = RNSMeshtasticInterface
    enabled = Yes
    mode = full

    transport = mqtt
    mesh_mode = broadcast
    mqtt_host = 127.0.0.1
    mqtt_port = 18883
    mqtt_root = rns-test/EU_868
    mqtt_channel = RNS-Test
    virtual_node_id = !b0000002
    mqtt_downlink_hops = 0
    mqtt_qos = 1
    tx_interval = 0

    network_name = rns-meshtastic-test
    passphrase = local-integration-test-only

